<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:WPFDevelopers.Controls"
    xmlns:helpers="clr-namespace:WPFDevelopers.Helpers"
    xmlns:resx="clr-namespace:WPFDevelopers">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Basic/ControlBasic.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style
        x:Key="WD.FilterListBoxItem"
        BasedOn="{StaticResource WD.DefaultListBoxItem}"
        TargetType="ListBoxItem">
        <Setter Property="Padding" Value="{Binding Padding, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource WD.RegularTextBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="helpers:ElementHelper.CornerRadius" Value="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        x:Name="PART_Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                        SnapsToDevicePixels="true">
                        <CheckBox
                            MinHeight="{TemplateBinding MinHeight}"
                            Margin="{Binding Padding, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            helpers:ElementHelper.CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                            Content="{Binding Value}"
                            Foreground="{TemplateBinding Foreground}"
                            IsChecked="{Binding IsChecked, Mode=TwoWay}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource WD.BaseBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
        x:Key="WD.Filter"
        BasedOn="{StaticResource WD.ControlBasicStyle}"
        TargetType="{x:Type controls:Filter}">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource WD.BaseBrush}" />
        <Setter Property="Background" Value="{DynamicResource WD.BackgroundBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="MaxHeight" Value="300" />
        <Setter Property="Padding" Value="{StaticResource WD.Padding}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Filter}">
                    <Border
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                        UseLayoutRounding="{TemplateBinding UseLayoutRounding}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBox
                                x:Name="PART_TextBox"
                                Margin="{TemplateBinding Padding}"
                                helpers:ElementHelper.CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                                helpers:ElementHelper.IsClear="True"
                                helpers:ElementHelper.Watermark="{Binding [InputWatermark], Source={x:Static resx:LanguageManager.Instance}}"
                                FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource TemplatedParent}}" />
                            <CheckBox
                                x:Name="PART_SelectAll"
                                Grid.Row="1"
                                Margin="{TemplateBinding Padding}"
                                helpers:ElementHelper.CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                                Content="{Binding [SelectAll], Source={x:Static resx:LanguageManager.Instance}}"
                                IsChecked="{Binding IsSelectAll, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Border
                                Grid.Row="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,1,0,0"
                                SnapsToDevicePixels="True"
                                UseLayoutRounding="True">
                                <ListBox
                                    x:Name="PART_ListBox"
                                    Padding="{TemplateBinding Padding}"
                                    helpers:ElementHelper.CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                                    BorderThickness="0"
                                    ItemContainerStyle="{StaticResource WD.FilterListBoxItem}"
                                    ItemsSource="{TemplateBinding Values}" />
                            </Border>
                            <Button
                                x:Name="PART_ApplyButton"
                                Grid.Row="3"
                                Width="60"
                                Margin="0,4"
                                HorizontalAlignment="Right"
                                helpers:ElementHelper.CornerRadius="{Binding Path=(helpers:ElementHelper.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                                Content="{Binding [Apply], Source={x:Static resx:LanguageManager.Instance}}"
                                Style="{StaticResource WD.PrimaryButton}"
                                ToolTip="{Binding [Apply], Source={x:Static resx:LanguageManager.Instance}}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource WD.Filter}" TargetType="{x:Type controls:Filter}" />
</ResourceDictionary>